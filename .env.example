# 环境变量配置示例
# 复制此文件为 .env.local 来覆盖默认配置

# API 配置
VITE_API_BASE_URL=http://localhost:52538

# 认证配置（true 或 false）
# - true: 启用 OIDC 认证（生产环境和测试完整流程时使用）
# - false: 禁用认证，直接访问（本地快速开发时使用）
#
# 注意：.env 文件默认为 true（安全）
# 如需本地开发时禁用认证，创建 .env.local 并设置为 false
VITE_ENABLE_AUTH=false
